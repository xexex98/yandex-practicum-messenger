<main class="registration">
  <h3>Регистрация</h3>
  <form
    action="#"
    class="registration-form"
  >
    <div>
      {{> Input label='Почта' name="email"}}
      {{> Input label='Логин' name='login'}}
      {{> Input label='Имя' name='first_name'}}
      {{> Input label='Фамилия' name='second_name'}}
      {{> Input label='Телефон' name='phone'}}
      {{> Input class="error" label='Пароль' name='password' type="password"}}
      {{> Input class="error" label='Пароль (еще раз)' name='password_check' error="Пароли не совпадают" required=true type="password"}}
    </div>
    <div>
      {{> Button text='Зарегистрироваться' type="submit"}}
      {{> Link href="#" class="registration-signin" label="Войти"}}
    </div>
  </form>
</main>
